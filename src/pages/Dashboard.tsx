import { Container, Typography } from '@mui/material'

export default function Dashboard() {
  return (
    <Container sx={{ py: 6 }}>
      <Typography variant="h5" gutterBottom>Dashboard</Typography>
      <Typography>List Meja & aksi buka order (placeholder)</Typography>
    </Container>
  )
}
